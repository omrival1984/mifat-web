# Mifat Corporate Website

Next.js App Router project generated from PLAN.md + DESIGN.md.

## Run locally
npm install
npm run dev

## Deploy
Upload repository to GitHub and import into Vercel. Root directory must contain `app`, `components`, `data`, and `package.json`.
